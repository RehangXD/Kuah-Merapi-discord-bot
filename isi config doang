from Config import MODULE_TOGGLES

self.module_name = "nama module"

@commands.Cog.listener()
  async def on_message(self, message):
    if not MODULE_TOGGLES.get(self.module_name, True):
      return